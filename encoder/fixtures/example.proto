syntax = "proto3";

package fixtures;

message TestMessage {
    int32 a = 1;
    int32 b = 2;
};

message TestMessageWithRepeated {
    repeated int32 a = 1;
};

message InnerMessage {
  int32 a = 1;
};

message TestMessageWithInner {
    InnerMessage inner = 1;
}

message TestRepeatedInner {
    repeated InnerMessage inner = 1;
};

service TestService {
  rpc TestMethod(TestMessage) returns (TestMessage);
};